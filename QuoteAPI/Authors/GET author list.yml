info:
  name: GET author list
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:5000/authors?sort_by=name
  params:
    - name: sort_by
      value: name
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
